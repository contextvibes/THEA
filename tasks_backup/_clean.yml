# tasks/_clean.yml
# This file defines the helper task for cleaning the local development environment.
# It orchestrates the process by calling the interactive menu script.

version: '3'

tasks:
  clean:
    desc: "(Helper) Runs the interactive cleaning menu script."
    silent: true
    cmds:
      - ./scripts/clean_menu.sh